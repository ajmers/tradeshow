# Copy this file to .env and fill in real values. .env is git-ignored.

AIRTABLE_PAT=

# Same Supabase project as the client's VITE_SUPABASE_URL / VITE_SUPABASE_PUBLISHABLE_KEY.
# Used to verify user session tokens and to look up each user's Airtable base id
# from the `profiles` table (see supabase/0001_profiles.sql).
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
