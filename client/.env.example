# Copy this file to .env.local and fill in real values. .env.local is git-ignored.
# These are Vite build-time env vars — must be prefixed VITE_ to reach the browser bundle.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
